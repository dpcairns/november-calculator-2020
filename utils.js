// declaration -- we're defining the input, output, and process
// "numbers in the abstract"
// recipe for making bread
// SINGLE RESPONSIBILITY PRINCIPLE
export function sum(booger1, booger2) {
    return booger1 + booger2;
}

export function subtract(number1, number2) {
    return number1 - number2;
}

// person 2 will write the math function